# 王举科研博客：使用说明

## 第一次上传

1. 打开 GitHub 仓库 `COOLWJ.github.io`。
2. 点击 `Add file` → `Upload files`。
3. 将本文件夹里的所有文件和文件夹一起拖入上传区域。
4. 点击页面底部的 `Commit changes`。
5. 打开仓库 `Settings` → `Pages`，选择 `Deploy from a branch`、`main` 和 `/ (root)`，然后保存。
6. 等待几分钟后访问 `https://coolwj.github.io`。

## 发布新文章

1. 进入仓库的 `_posts` 文件夹。
2. 点击 `Add file` → `Create new file`。
3. 文件名按照 `年-月-日-英文标题.md`，例如 `2026-08-25-liquid-test.md`。
4. 复制示例文章顶部的格式，修改标题、日期和分类，再填写正文。
5. 点击 `Commit changes`，稍等片刻，文章会自动出现在首页。

## 修改个人资料

- 修改首页：编辑 `index.html`。
- 修改“关于我”：编辑 `about.md`。
- 修改网站名称：编辑 `_config.yml`。
- 删除示例文章：删除 `_posts/2026-08-23-my-first-post.md`。

